<nav>
	<% if user_signed_in? %>
	  <%= link_to "Edit Password", edit_user_account_password_path %>
	  <%= button_to "Log out", user_sessions_path, method: :delete %>
	<% else %>
	  <%= link_to "Sign Up", new_user_registration_path %>
	  <%= link_to "Log in", login_path %>
	<% end %>
</nav>

<style>
	nav {
		display: flex;
		align-items: center;
		justify-content: flex-end;
		padding-bottom: 10px;
		border-bottom: 1px solid;
	}

	nav a {
		margin-right: 10px;
	}
</style>